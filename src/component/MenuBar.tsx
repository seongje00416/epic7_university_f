import React from 'react';
import { MenuNavBar, LogoDiv } from '@/style/component/Style_MenuBar';

const MenuBar: React.FC = () => {

    return (
        <MenuNavBar>
            <LogoDiv> Epic Seven University </LogoDiv>
        </MenuNavBar>
    );
};

export default MenuBar;